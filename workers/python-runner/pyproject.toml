[project]
name = "devdex-python-runner"
version = "0.1.0"
description = "Backend-authoritative Python mission runner for DevDex"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
